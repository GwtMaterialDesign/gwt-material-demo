<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
             xmlns:g="urn:import:com.google.gwt.user.client.ui"
             xmlns:m="urn:import:gwt.material.design.client.ui">
    <ui:style>
        .btn {
            width: 200px;
        }
    </ui:style>
    <g:HTMLPanel>
        <m:MaterialTitle title="Toast"
                         description="Materialize provides an easy way for you to send unobtrusive alerts to your users through toasts. These toasts are also placed and sized responsively, try it out by clicking the button below on different device sizes."/>
        <m:MaterialButton text="Toast Me" ui:field="btnToast" color="blue" addStyleNames="{style.btn}"/>
        <pre>
            MaterialToast.alert(<span class="tag">"I Love Material Design"</span>);
        </pre>

        <m:MaterialTitle title="Modal"
                         description="Use a modal for dialog boxes, confirmation messages, or other content that can be called up. In order for the modal to work you have to add the Modal ID to the link of the trigger. "/>
        <m:MaterialButton text="Show Modal" ui:field="btnModal" color="blue" addStyleNames="{style.btn}"/>
        <m:MaterialButton text="Modal with Fix Footer" ui:field="btnModalFixFooter" color="blue"/>
        <pre>
            MaterialModal.showModal(<span class="tag">true</span>,
            <span class="tag">new</span>
            ModalContent());
        </pre>

        <m:MaterialTitle title="Tooltip"
                         description="Tooltips are small, interactive, textual hints for mainly graphical elements. When using icons for actions you can use a tooltip to give people clarification on its function."/>
        <m:MaterialButton text="Bottom" tooltip="I am tooltip" tooltipLocation="bottom" color="blue"
                          addStyleNames="{style.btn}"/>
        <m:MaterialButton text="Top" tooltip="I am tooltip" tooltipLocation="top" color="blue"
                          addStyleNames="{style.btn}"/>
        <m:MaterialButton text="Left" tooltip="I am tooltip" tooltipLocation="left" color="blue"
                          addStyleNames="{style.btn}"/>
        <m:MaterialButton text="Right" tooltip="I am tooltip" tooltipLocation="right" color="blue"
                          addStyleNames="{style.btn}"/>
    </g:HTMLPanel>
</ui:UiBinder>